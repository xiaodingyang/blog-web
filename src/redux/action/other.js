import { createActions } from 'redux-actions';
// import { getList } from '../../servers/imgs';

export default createActions({
    GET_IS_DRAWER: (params) => params,
    SET_LOADING: (params) => params,
});
